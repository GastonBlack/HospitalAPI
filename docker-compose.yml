services:
  api:
    build: .
    container_name: hospital-api
    ports:
      - "5095:8080"
    environment:
      ASPNETCORE_URLS: http://+:8080
      ASPNETCORE_ENVIRONMENT: Development
      ConnectionStrings__DefaultConnection: Host=postgres;Port=5432;Database=HospitalDb;Username=postgres;Password=postgres
    depends_on:
      postgres:
        condition: service_healthy

  postgres:
    image: postgres:18
    container_name: hospital-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: HospitalDb
    ports:
      - "5433:5432"
    volumes:
      - hospital-postgres-data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d HospitalDb"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  hospital-postgres-data:
